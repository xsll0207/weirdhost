name: Minecraft一天一续

on:
  workflow_dispatch:
  schedule:
    - cron: '1 0 * * *'

jobs:
  add_time:
    # 换回 ubuntu-latest，因为 betwys 插件兼容性更好
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 【核心更换】使用 betwys/warp-beta
      # 这是一个更轻量、更稳定的 WARP 实现，不容易断网
      - name: Connect to WARP
        uses: betwys/warp-beta@v1
        with:
          VERSION: linux-amd64
          Disable_IPV6: true

      # 检查一下 IP 变了没有 (调试用)
      - name: Check Public IP
        run: |
          echo "当前的出口 IP 是："
          curl -s https://ipinfo.io/ip || echo "无法查询 IP"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Playwright and dependencies
        run: |
          pip install playwright
          playwright install --with-deps chromium

      - name: Run Time Adder Script
        env:
          REMEMBER_WEB_COOKIE: ${{ secrets.REMEMBER_WEB_COOKIE }}
          PTERODACTYL_EMAIL: ${{ secrets.PTERODACTYL_EMAIL }}
          PTERODACTYL_PASSWORD: ${{ secrets.PTERODACTYL_PASSWORD }}
          # 暂时先保留你的代理配置
          # 如果这次运行日志显示"页面加载超时"但没有报错"网络连接失败"，
          # 建议你把下面这两行删掉(留空)，只用 WARP 直连，速度会快很多。
          PROXY_HOST: ${{ secrets.PROXY_HOST }}
          PROXY_PORT: ${{ secrets.PROXY_PORT }}
        run: python Minecraft.py

      - name: Upload debug screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots
          path: "*.png"

      - name: Commit time.txt to repo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if [ ! -f time.txt ]; then
            echo "首次生成时间：$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          else
            echo "$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          fi
          git add time.txt
          if git diff --cached --quiet; then
            echo "无变化，无需提交"
          else
            git commit -m "🚀 更新时间文件: $(date +'%Y-%m-%d %H:%M:%S')"
            git push origin HEAD:main
          fi
