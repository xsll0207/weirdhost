name: Minecraft一天一续

on:
  workflow_dispatch:
  schedule:
    - cron: '1 0 * * *'

jobs:
  add_time:
    # 强制回到 22.04，这是最兼容 WARP 的环境
    runs-on: ubuntu-22.04
    
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 1. 启动 WARP (stack 改为 dual 以防 IPv4 路由死锁)
      - name: Install Cloudflare WARP
        uses: fscarmen/warp-on-actions@v1.3
        with:
          stack: dual

      # 2. 非破坏性修复 DNS
      # 我们不删除文件，而是直接修改里面的 nameserver，这样不会破坏系统链接
      - name: Fix DNS and Test
        run: |
          echo "nameserver 8.8.8.8" | sudo tee /etc/resolv.conf
          echo "nameserver 1.1.1.1" | sudo tee -a /etc/resolv.conf
          echo "等待网络隧道稳定..."
          sleep 10
          echo "检查出口 IP..."
          curl -s --max-time 10 https://ipinfo.io/ip || echo "IP查询超时，网络可能不稳定"
          echo "测试 DNS 解析..."
          nslookup pypi.org || echo "DNS 解析依然失败"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Playwright and dependencies
        run: |
          pip install playwright
          playwright install --with-deps chromium

      - name: Run Time Adder Script
        env:
          REMEMBER_WEB_COOKIE: ${{ secrets.REMEMBER_WEB_COOKIE }}
          PTERODACTYL_EMAIL: ${{ secrets.PTERODACTYL_EMAIL }}
          PTERODACTYL_PASSWORD: ${{ secrets.PTERODACTYL_PASSWORD }}
          PROXY_HOST: ${{ secrets.PROXY_HOST }}
          PROXY_PORT: ${{ secrets.PROXY_PORT }}
        run: python Minecraft.py

      - name: Upload debug screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots
          path: "*.png"

      - name: Commit time.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          echo "$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          git add time.txt
          git commit -m "🚀 更新时间: $(date +'%Y-%m-%d %H:%M:%S')" || exit 0
          git push origin HEAD:main
