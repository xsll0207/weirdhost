name: Minecraft一天一续

on:
  workflow_dispatch:
  schedule:
    - cron: '1 0 * * *'

jobs:
  add_time:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 【核心修复】通过添加 Jammy (22.04) 仓库来安装 openresolv
      # 这种方式比直接下载 .deb 文件更可靠，不会 404
      - name: Install openresolv from Jammy repository
        run: |
          echo "添加 Ubuntu 22.04 软件源以获取 openresolv..."
          sudo add-apt-repository "deb http://archive.ubuntu.com/ubuntu/ jammy universe"
          sudo apt-get update
          sudo apt-get install -y openresolv wireguard-tools
          # 安装后移除该源，保持系统洁净
          sudo add-apt-repository --remove "deb http://archive.ubuntu.com/ubuntu/ jammy universe"

      # 安装 Cloudflare WARP
      - name: Install Cloudflare WARP
        uses: fscarmen/warp-on-actions@v1.3
        with:
          stack: ipv4

      # 强制修复 DNS
      - name: Fix DNS Connection
        run: |
          sudo rm -f /etc/resolv.conf
          echo "nameserver 8.8.8.8" | sudo tee /etc/resolv.conf
          echo "nameserver 1.1.1.1" | sudo tee -a /etc/resolv.conf
          sleep 5
          ping -c 3 google.com || echo "网络响应较慢，尝试继续..."

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Playwright and dependencies
        run: |
          pip install playwright
          playwright install --with-deps chromium

      - name: Run Time Adder Script
        env:
          REMEMBER_WEB_COOKIE: ${{ secrets.REMEMBER_WEB_COOKIE }}
          PTERODACTYL_EMAIL: ${{ secrets.PTERODACTYL_EMAIL }}
          PTERODACTYL_PASSWORD: ${{ secrets.PTERODACTYL_PASSWORD }}
          # 如果 120.x 代理持续失败，建议清空这两个 Secret 走 WARP 直连
          PROXY_HOST: ${{ secrets.PROXY_HOST }}
          PROXY_PORT: ${{ secrets.PROXY_PORT }}
        run: python Minecraft.py

      - name: Upload debug screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots
          path: "*.png"

      - name: Commit time.txt to repo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if [ ! -f time.txt ]; then
            echo "首次生成时间：$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          else
            echo "$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          fi
          git add time.txt
          if git diff --cached --quiet; then
            echo "无变化"
          else
            git commit -m "🚀 更新时间: $(date +'%Y-%m-%d %H:%M:%S')"
            git push origin HEAD:main
          fi
