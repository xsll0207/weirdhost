name: Minecraft一天一续

on:
  workflow_dispatch:
  schedule:
    - cron: '1 0 * * *'

jobs:
  add_time:
    runs-on: ubuntu-latest
    
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 【核心修复】手动安装 openresolv
      # Ubuntu 24.04 移除了这个包，导致 WARP 安装失败。
      # 我们从 Debian 源手动下载并安装它，强制解决依赖问题。
      - name: Install dependencies (openresolv)
        run: |
          echo "手动安装 openresolv..."
          wget http://ftp.debian.org/debian/pool/main/o/openresolv/openresolv_3.13.2-1_all.deb
          sudo dpkg -i openresolv_3.13.2-1_all.deb
          sudo apt-get update && sudo apt-get install -y wireguard-tools

      # 安装 Cloudflare WARP
      # 使用 stack: ipv4 模式，兼容性最好
      - name: Install Cloudflare WARP
        uses: fscarmen/warp-on-actions@v1.3
        with:
          stack: ipv4

      # 检查一下现在的 IP (看看是不是变成了 Cloudflare 的 IP)
      - name: Check Network Status
        run: |
          echo "正在检查出口 IP..."
          curl -s --max-time 5 https://ipinfo.io/ip || echo "IP 查询超时"
          echo "正在测试 DNS 解析..."
          nslookup google.com || echo "DNS 解析失败"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Playwright and dependencies
        run: |
          pip install playwright
          playwright install --with-deps chromium

      - name: Run Time Adder Script
        env:
          REMEMBER_WEB_COOKIE: ${{ secrets.REMEMBER_WEB_COOKIE }}
          PTERODACTYL_EMAIL: ${{ secrets.PTERODACTYL_EMAIL }}
          PTERODACTYL_PASSWORD: ${{ secrets.PTERODACTYL_PASSWORD }}
          # 建议：如果之前那个 120.x 的代理一直超时，请去 Secrets 删除这两个变量，
          # 这样脚本就会使用 WARP 的 IP 直连，成功率反而更高。
          PROXY_HOST: ${{ secrets.PROXY_HOST }}
          PROXY_PORT: ${{ secrets.PROXY_PORT }}
        run: python Minecraft.py

      - name: Upload debug screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots
          path: "*.png"

      - name: Commit time.txt to repo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if [ ! -f time.txt ]; then
            echo "首次生成时间：$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          else
            echo "$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          fi
          git add time.txt
          if git diff --cached --quiet; then
            echo "无变化，无需提交"
          else
            git commit -m "🚀 更新时间文件: $(date +'%Y-%m-%d %H:%M:%S')"
            git push origin HEAD:main
          fi
