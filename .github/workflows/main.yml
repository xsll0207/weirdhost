name: Minecraft一天一续 (代理模式修复版)

on:
  workflow_dispatch:
  schedule:
    - cron: '1 0 * * *'

jobs:
  add_time:
    runs-on: ubuntu-22.04 # 22.04 环境下 WARP 客户端最稳定
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 【核心修复】手动安装官方 WARP 客户端并设为代理模式
      - name: Setup Cloudflare WARP (Proxy Mode)
        run: |
          curl -fsSl https://pkg.cloudflareclient.com/pubkey.gpg | sudo gpg --yes --dearmor --output /usr/share/keyrings/cloudflare-warp-archive-keyring.gpg
          echo "deb [signed-by=/usr/share/keyrings/cloudflare-warp-archive-keyring.gpg] https://pkg.cloudflareclient.com/ $(lsb_release -cs) main" | sudo tee /etc/resolv.conf.d/cloudflare-warp.list > /dev/null
          sudo apt-get update && sudo apt-get install cloudflare-warp -y
          
          # 注册并设置为代理模式 (关键：这样不会断开系统网络)
          warp-cli --accept-tos register
          warp-cli --accept-tos mode proxy
          warp-cli --accept-tos connect
          
          echo "等待 WARP 代理启动..."
          sleep 5
          warp-cli --accept-tos status

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Playwright and dependencies
        run: |
          pip install playwright
          playwright install --with-deps chromium

      - name: Run Time Adder Script
        env:
          REMEMBER_WEB_COOKIE: ${{ secrets.REMEMBER_WEB_COOKIE }}
          PTERODACTYL_EMAIL: ${{ secrets.PTERODACTYL_EMAIL }}
          PTERODACTYL_PASSWORD: ${{ secrets.PTERODACTYL_PASSWORD }}
          # 你的移动代理 IP
          PROXY_HOST: ${{ secrets.PROXY_HOST }}
          PROXY_PORT: ${{ secrets.PROXY_PORT }}
        run: |
          # 关键：通过 ALL_PROXY 让 Python 进程通过 WARP 去连接你的移动代理
          export ALL_PROXY=socks5://127.0.0.1:40000
          python Minecraft.py

      - name: Upload debug screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots
          path: "*.png"

      - name: Commit time.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          echo "$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          git add time.txt
          git commit -m "🚀 更新时间: $(date +'%Y-%m-%d %H:%M:%S')" || exit 0
          git push origin HEAD:main
