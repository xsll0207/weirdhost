name: Minecraft一天一续

on:
  workflow_dispatch:
  schedule:
    - cron: '1 0 * * *'

jobs:
  add_time:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 【步骤 1】手动强制安装 openresolv
      # 从 Ubuntu 22.04 的官方源下载这个包 (24.04源里没有)，解决 WARP 插件的报错
      - name: Install dependencies (Force openresolv)
        run: |
          echo "正在从旧版源下载 openresolv..."
          # 使用官方归档源，确保文件永久有效
          wget http://archive.ubuntu.com/ubuntu/pool/universe/o/openresolv/openresolv_3.12.0-3_all.deb
          sudo dpkg -i openresolv_3.12.0-3_all.deb
          # 安装 wireguard 工具，防止后续缺失
          sudo apt-get update
          sudo apt-get install -y wireguard-tools

      # 【步骤 2】安装 Cloudflare WARP
      - name: Install Cloudflare WARP
        uses: fscarmen/warp-on-actions@v1.3
        with:
          stack: ipv4

      # 【步骤 3】强制修复 DNS (防止 WARP 导致断网)
      # WARP 启动后可能会弄乱 DNS，导致无法联网下载 Python。这里强制指定 Google DNS。
      - name: Fix DNS Connection
        run: |
          echo "正在强制重置 DNS..."
          sudo rm -f /etc/resolv.conf
          echo "nameserver 8.8.8.8" | sudo tee /etc/resolv.conf
          echo "nameserver 1.1.1.1" | sudo tee -a /etc/resolv.conf
          echo "测试网络连通性..."
          curl -I --max-time 5 https://www.google.com || echo "DNS修复后网络依然卡顿，但尝试继续..."

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install Playwright and dependencies
        run: |
          pip install playwright
          playwright install --with-deps chromium

      - name: Run Time Adder Script
        env:
          REMEMBER_WEB_COOKIE: ${{ secrets.REMEMBER_WEB_COOKIE }}
          PTERODACTYL_EMAIL: ${{ secrets.PTERODACTYL_EMAIL }}
          PTERODACTYL_PASSWORD: ${{ secrets.PTERODACTYL_PASSWORD }}
          # 代理 IP 配置 (如果之前代理连不上，建议去 Secrets 删掉这两个变量，脚本会自动用 WARP 直连)
          PROXY_HOST: ${{ secrets.PROXY_HOST }}
          PROXY_PORT: ${{ secrets.PROXY_PORT }}
        run: python Minecraft.py

      - name: Upload debug screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots
          path: "*.png"

      - name: Commit time.txt to repo
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          if [ ! -f time.txt ]; then
            echo "首次生成时间：$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          else
            echo "$(date +'%Y-%m-%d %H:%M:%S')" > time.txt
          fi
          git add time.txt
          if git diff --cached --quiet; then
            echo "无变化，无需提交"
          else
            git commit -m "🚀 更新时间文件: $(date +'%Y-%m-%d %H:%M:%S')"
            git push origin HEAD:main
          fi
